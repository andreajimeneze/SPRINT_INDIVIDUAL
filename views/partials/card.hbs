{{#each dataPdto }}
<div class="row row-cols-2 row-cols-md-3 g-4 cardPdto">
            <div class="col">
            <div class="card h-100">
                <img src="{{ img }}" class="card-img-top imgPdto" alt="...">
                <div class="card-body">
                <h5 class="card-title"> {{ nombre }}</h5>
                <p class="card-text">Código: {{ id }}</p>
                <p class="card-text">Precio $: {{ precio}}</p>
                </div>
                <div class="d-flex column justify-content-center" class="tarjeta-modificar">
                <button class="btn btn-info" value="{{ id }}">-</button>
                <p class="mb-0 mt-2 align-self:center" id="{{ id }}" >1</p>
                <button class="btn btn-info" value="{{ id }}">+</button>
            </div>
            <div class="agregarCarro" class="d-flex">
                <button class="btn btn-primary" value= "{{ id }}" key=nada id="btnAdd_{{ id }}">Agregar</button>
            </div>
            </div>
            </div>
        </div>
{{/each}}